import type {Attempt,CurriculumMetadata,Evaluation,PedagogicalResource,ProblemDetail,ProblemSource,ProblemSummary,ResolvedResourcesResponse,SelectionResult,TutorAssessment,TutorTrigger} from "./types";
const API_BASE="/api";
export class ApiError extends Error{constructor(public status:number,public payload?:{error?:string;detail?:string;current_revision?:number},options?:ErrorOptions){super(payload?.error??payload?.detail??`API request failed (${status})`,options);this.name="ApiError"}}
export async function apiFetch<T>(path:string,init:RequestInit&{timeoutMs?:number}={}):Promise<T>{
  const controller=new AbortController(),timeoutMs=init.timeoutMs??15_000;
  const timeout=setTimeout(()=>controller.abort(new DOMException("Request timed out","TimeoutError")),timeoutMs);
  const abort=()=>controller.abort(init.signal?.reason);init.signal?.addEventListener("abort",abort,{once:true});
  let response:Response;
  try{response=await fetch(`${API_BASE}${path}`,{...init,signal:controller.signal,headers:{Accept:"application/json",...init.headers}})}
  catch(error){throw new ApiError(0,{error:error instanceof DOMException&&error.name==="TimeoutError"?"Request timed out":"Network request failed"},{cause:error})}
  finally{clearTimeout(timeout);init.signal?.removeEventListener("abort",abort)}
  const text=await response.text();let data:unknown=undefined;
  if(text)try{data=JSON.parse(text)}catch{if(response.ok)throw new ApiError(response.status,{error:"Invalid JSON response"})}
  if(!response.ok){const payload=data&&typeof data==="object"?data as {error?:string;detail?:string;current_revision?:number}:{error:text?`HTTP error (${response.status})`:`Empty error response (${response.status})`};throw new ApiError(response.status,payload)}
  return data as T;
}
type WireSummary={id:string;title:string;subtitle?:string;curriculum:{level:string;difficulty:number};estimated_minutes?:number;year?:number;topics:string[];source:ProblemSource};type WireDetail=WireSummary&{statement:string;hint_levels:number[];prerequisites:string[];skills:string[];resources?:ProblemDetail["resources"]};
const request=<T>(path:string,init?:RequestInit)=>apiFetch<T>(path,init);function summary(value:WireSummary):ProblemSummary{return{id:value.id,title:value.title,subtitle:value.subtitle,curriculum:value.curriculum,estimatedMinutes:value.estimated_minutes,year:value.year,topics:value.topics,source:value.source}}
export async function getProblems(){return(await request<WireSummary[]>("/problems")).map(summary)}export async function getProblem(id:string):Promise<ProblemDetail>{const value=await request<WireDetail>(`/problems/${encodeURIComponent(id)}`);return{...summary(value),statement:value.statement,hintLevels:value.hint_levels,prerequisites:value.prerequisites,skills:value.skills,resources:value.resources}}
export const getCurriculum=()=>request<CurriculumMetadata>("/curriculum");export type HealthStatus={status:"ok";database:"ok";problem_corpus:"ok";problem_count:number;resource_corpus:"ok";resource_count:number;curriculum_levels:number};export const getHealth=()=>request<HealthStatus>("/health");export const getProblemResources=(id:string)=>request<ResolvedResourcesResponse>(`/problems/${encodeURIComponent(id)}/resources`);export const getResource=(id:string)=>request<PedagogicalResource>(`/resources/${encodeURIComponent(id)}`);
export type InferenceStatus={provider:string;status:"disabled"|"ready"|"unavailable"|"error";family:string;fast_model:string;fast_backend:string;deep_model:string;deep_backend:string;latency_ms?:number;reason?:string|null};export const getInferenceStatus=(refresh=false)=>request<InferenceStatus>(`/inference/status${refresh?"?refresh=true":""}`);export type InferenceDiagnostics=InferenceStatus&{checks:{provider_config:string;router:string;authentication:string;structured_output:string}};
const diagnosticRequest=<T>(path:string,token:string,method="GET")=>request<T>(path,{method,headers:{"X-Diagnostics-Token":token}});export const getDiagnostics=(token:string)=>diagnosticRequest<InferenceDiagnostics>("/diagnostics/inference",token);export const getDiagnosticLogs=(source:"application"|"inference",token:string,lines=200)=>diagnosticRequest<{source:string;lines:string[];available:boolean}>(`/diagnostics/logs?source=${source}&lines=${lines}`,token);export type InferenceTestResult={status:string;latency_ms?:number;response_preview?:string;provider?:string;model?:string;tokens?:number;estimated_cost_usd?:number|null;reason?:string;error_code?:string};export const testInference=(token:string,role:"fast"|"deep")=>diagnosticRequest<InferenceTestResult>(`/diagnostics/inference/test?role=${role}`,token,"POST");
export async function selectProblem(level:string,difficulty:number,topic?:string,exclude?:string[]):Promise<SelectionResult>{const query=new URLSearchParams({level,difficulty:String(difficulty)});if(topic)query.append("topic",topic);exclude?.forEach(id=>query.append("exclude",id));const value=await request<{problem:WireDetail|null;requested_level:string;requested_difficulty?:number;actual_difficulty?:number;fallback_used:boolean}>(`/problems/select?${query}`);return{...value,problem:value.problem?{...summary(value.problem),statement:value.problem.statement,hintLevels:value.problem.hint_levels,prerequisites:value.problem.prerequisites,skills:value.problem.skills,resources:value.problem.resources}:null}}
const json={"Content-Type":"application/json"};const attemptRequest=(path:string,init?:RequestInit)=>request<Attempt>(path,{...init,headers:{...json,...init?.headers}});export const createAttempt=(problemId:string)=>attemptRequest("/attempts",{method:"POST",body:JSON.stringify({problem_id:problemId})});export const getAttempt=(id:string)=>attemptRequest(`/attempts/${id}`);export const patchAttempt=(id:string,solution:string,elapsed:number,revision:number)=>attemptRequest(`/attempts/${id}`,{method:"PATCH",body:JSON.stringify({solution_markdown:solution,elapsed_seconds:elapsed,expected_revision:revision})});export const submitAttempt=(id:string,revision:number)=>attemptRequest(`/attempts/${id}/submit`,{method:"POST",body:JSON.stringify({expected_revision:revision})});
const evaluationRequest=(path:string,method="GET")=>request<Evaluation>(path,{method,headers:json});export const createEvaluation=(id:string)=>evaluationRequest(`/attempts/${id}/evaluation`,"POST");export const getEvaluation=(id:string)=>evaluationRequest(`/attempts/${id}/evaluation`);export const retryEvaluation=(id:string)=>evaluationRequest(`/attempts/${id}/evaluation/retry`,"POST");export type ReferenceSolution={problem_id:string;title:string;reference_solution:string};export const getReferenceSolution=(id:string)=>request<ReferenceSolution>(`/attempts/${id}/reference-solution`);
const tutorRequest=(path:string,init?:RequestInit)=>request<TutorAssessment|null>(path,{...init,headers:json});export const assessTutor=(id:string,revision:number,trigger:TutorTrigger,level:number,clientRequestId=crypto.randomUUID())=>tutorRequest(`/attempts/${id}/tutor/assess`,{method:"POST",body:JSON.stringify({expected_revision:revision,trigger,requested_help_level:level,client_request_id:clientRequestId})});export const getLatestTutor=(id:string)=>tutorRequest(`/attempts/${id}/tutor/latest`);
