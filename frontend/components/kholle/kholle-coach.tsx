"use client";
import {useEffect,useRef,useState} from "react";
const steps=[{title:"Énoncé",body:"Lis l’énoncé ici. Tu peux le replier pour agrandir ton espace de travail."},{title:"Tableau",body:"Construis ton raisonnement ici. Le professeur s’intéresse à ta démarche, pas seulement au résultat."},{title:"Professeur",body:"Le professeur observe ta progression et intervient si tu bloques."}];
export function KholleCoach({onClose}:{onClose:()=>void}){const[index,setIndex]=useState(0),button=useRef<HTMLButtonElement>(null);useEffect(()=>{button.current?.focus();const key=(event:KeyboardEvent)=>{if(event.key==="Escape")onClose()};window.addEventListener("keydown",key);return()=>window.removeEventListener("keydown",key)},[onClose]);const step=steps[index];return <div className="coach-backdrop"><section className="coach-dialog" role="dialog" aria-modal="true" aria-labelledby="coach-title"><p className="eyebrow">Repère {index+1} sur 3</p><h2 id="coach-title">{step.title}</h2><p>{step.body}</p><div className="coach-actions"><button className="secondary" onClick={onClose}>Passer le guide</button><button ref={button} onClick={()=>index===2?onClose():setIndex(value=>value+1)}>{index===2?"Commencer":"Suivant"}</button></div></section></div>}
