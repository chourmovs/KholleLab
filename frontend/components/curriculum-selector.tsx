import type {CurriculumMetadata} from "@/lib/types";
export function CurriculumSelector({metadata,level,difficulty,topic,topics,onLevel,onDifficulty,onTopic}:{metadata:CurriculumMetadata;level:string;difficulty:number;topic:string;topics:string[];onLevel:(v:string)=>void;onDifficulty:(v:number)=>void;onTopic:(v:string)=>void}){return <section className="curriculum-selector"><fieldset><legend>Quel niveau souhaitez-vous travailler ?</legend>{metadata.levels.map(item=><button className={level===item.id?"active":""} key={item.id} onClick={()=>onLevel(item.id)}>{item.label}</button>)}</fieldset><fieldset><legend>Quelle intensité ?</legend>{metadata.difficulties.map(item=><button className={difficulty===item.id?"active":""} key={item.id} onClick={()=>onDifficulty(item.id)}>{item.id} {item.label}</button>)}</fieldset><label>Thème <select aria-label="Thème" value={topic} onChange={event=>onTopic(event.target.value)}><option value="">Tous les thèmes</option>{topics.map(value=><option key={value} value={value}>{value}</option>)}</select></label></section>}
