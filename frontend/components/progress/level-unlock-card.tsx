export function LevelUnlockCard({nextLabel,currentLabel,busy,error,onAdvance,onConsolidate}:{nextLabel:string;currentLabel:string;busy:boolean;error?:string;onAdvance:()=>void;onConsolidate:()=>void}){
 return <section className="level-unlock" aria-labelledby="level-unlock-title"><h3 id="level-unlock-title">🎉 Niveau {nextLabel} débloqué</h3><p>Tu as validé au moins 60 % du parcours de {currentLabel}.</p><p>Tu peux passer en {nextLabel} maintenant, ou continuer à consolider la {currentLabel}.</p>{error&&<p className="progress-error" role="alert">{error}</p>}<div className="progress-actions"><button disabled={busy} onClick={onAdvance} aria-label={`Passer au niveau ${nextLabel}`}>{busy?"Changement…":`Passer en ${nextLabel}`}</button><button className="secondary" disabled={busy} onClick={onConsolidate} aria-label={`Continuer au niveau ${currentLabel}`}>{`Continuer en ${currentLabel}`}</button></div></section>
}
