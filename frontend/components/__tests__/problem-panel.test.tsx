import {render,screen} from "@testing-library/react";import {describe,expect,it} from "vitest";import {ProblemPanel} from "../problem-panel";
const problem={id:"demo-001",title:"Une somme",level:"premiere",difficulty:2,estimatedMinutes:20,topics:["algebra"],source:{type:"internal",name:"Corpus test"},statement:"Calculer \\(x^2\\).",hintLevels:[1]};
describe("ProblemPanel",()=>{it("renders metadata and KaTeX mathematics",()=>{const {container}=render(<ProblemPanel problem={problem}/>);expect(screen.getByText("Une somme")).toBeInTheDocument();expect(screen.getByText("Première")).toBeInTheDocument();expect(screen.getByText("Algèbre")).toBeInTheDocument();expect(screen.getByLabelText(/Difficulté 2 sur 5/)).toBeInTheDocument();expect(container.querySelector(".katex")).toBeInTheDocument()})})
