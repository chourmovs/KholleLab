import {fireEvent,render,screen} from "@testing-library/react";import {describe,expect,it} from "vitest";import {ProblemPanel} from "../problem-panel";
const problem={id:"demo-001",title:"Une somme",curriculum:{level:"premiere",difficulty:2},estimatedMinutes:20,topics:["algebra"],source:{type:"internal",name:"Corpus test"},statement:"Calculer \\(x^2\\).",hintLevels:[1],prerequisites:[],skills:[]};
describe("ProblemPanel",()=>{it("renders metadata and KaTeX mathematics",()=>{const {container}=render(<ProblemPanel problem={problem}/>);expect(screen.getByText("Une somme")).toBeInTheDocument();expect(screen.getByText(/premiere/)).toBeInTheDocument();expect(screen.getByText("Algèbre")).toBeInTheDocument();expect(screen.getByLabelText(/Difficulté 2 sur 5/)).toBeInTheDocument();expect(container.querySelector(".katex")).toBeInTheDocument()});it("hides unavailable resource actions",()=>{render(<ProblemPanel problem={problem}/>);expect(screen.queryByText(/Point de cours/)).not.toBeInTheDocument();expect(screen.queryByText(/Vidéo/)).not.toBeInTheDocument()});it("opens curated resources on demand without changing the draft",()=>{localStorage.setItem("draft","copie inchangée");const enriched={...problem,resources:{course_points:[{title:"Identité",summary:"\\(a^2-b^2=(a-b)(a+b)\\)",topics:["algebra"]}],videos:[{title:"Comprendre",provider:"youtube" as const,url:"https://youtu.be/abc",author:"Prof",duration_minutes:8}]}};const {container}=render(<ProblemPanel problem={enriched}/>);fireEvent.click(screen.getByText(/Point de cours/));expect(screen.getByRole("dialog",{name:"Point de cours"})).toBeInTheDocument();expect(container.querySelector(".resource-modal .katex")).toBeInTheDocument();fireEvent.click(screen.getByLabelText("Fermer"));fireEvent.click(screen.getByText(/Vidéo 8 min/));expect(screen.getByRole("link",{name:"Voir la vidéo"})).toHaveAttribute("href","https://youtu.be/abc");expect(localStorage.getItem("draft")).toBe("copie inchangée")})})
