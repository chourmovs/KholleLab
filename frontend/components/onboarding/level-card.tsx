import type {CurriculumLevelMetadata} from "@/lib/types";
const GUIDANCE:Record<string,string>={quatrieme:"Consolider les bases du collège et progresser dans le Cycle 4.",troisieme:"Préparer la fin du collège et l’entrée au lycée.",seconde:"Commencer ou reprendre le programme mathématique du lycée.",premiere:"Travailler le programme de Première et gagner en autonomie.",terminale:"Préparer le programme de Terminale et les raisonnements plus avancés.","maths-sup":"Entrer dans le rythme et les méthodes des mathématiques de CPGE.","maths-spe":"Travailler au niveau Maths Spé avec des problèmes plus exigeants."};
export function LevelCard({level,selected,disabled,onSelect}:{level:CurriculumLevelMetadata;selected:boolean;disabled:boolean;onSelect:()=>void}){const domains=level.domains.map(x=>x.label).slice(0,4).join(" · ");return <button type="button" role="radio" aria-checked={selected} disabled={disabled} className={`level-card${selected?" selected":""}`} onClick={onSelect}><span className="level-card-title"><strong>{level.label}</strong><b>{level.short_label}</b></span><span className="programme">{level.programme.label}</span>{domains&&<span className="domains">{domains}{level.domains.length>4?"…":""}</span>}<span>{GUIDANCE[level.id]??`Commencer le programme de ${level.label}.`}</span><span className="selection">{selected?"Niveau sélectionné":"Choisir ce niveau"}</span></button>}
