import type {ReactNode} from "react";
import {Eye,Keyboard,Send} from "lucide-react";
export function WorkspaceLayout({statement,blackboard,tools,professor}:{statement:ReactNode;blackboard:ReactNode;tools:ReactNode;professor:ReactNode}){return <div className="workspace-layout" data-testid="workspace"><div className="statement-pane">{statement}</div><div className="blackboard-pane">{blackboard}</div>{tools}<div className="professor-pane">{professor}</div></div>}
export function BlackboardToolRail({onKeyboard,onPreview,onSubmit}:{onKeyboard:()=>void;onPreview:()=>void;onSubmit:()=>void}){const items=[{Icon:Keyboard,label:"Clavier",action:onKeyboard},{Icon:Eye,label:"Aperçu",action:onPreview},{Icon:Send,label:"Présenter ma solution",action:onSubmit,primary:true}];return <nav className="tool-rail" aria-label="Outils du tableau">{items.map(({Icon,label,action,primary})=><button className={primary?"primary":""} key={label} type="button" title={label} aria-label={label} onClick={action}><Icon aria-hidden="true"/><span>{label}</span></button>)}</nav>}
