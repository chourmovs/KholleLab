import type {ReactNode} from "react";
export function WorkspaceLayout({statement,blackboard,tools}:{statement:ReactNode;blackboard:ReactNode;tools:ReactNode}){return <div className="workspace-stack"><div className="statement-pane">{statement}</div><div className="workspace-layout"><div className="blackboard-pane">{blackboard}</div>{tools}</div></div>}
export function BlackboardToolRail({onKeyboard,onPreview,onSubmit}:{onKeyboard:()=>void;onPreview:()=>void;onSubmit:()=>void}){const items=[["⌨","Clavier scientifique",onKeyboard,false],["📘","Point de cours",()=>{},true],["▶","Vidéo",()=>{},true],["💡","Indice — bientôt disponible",()=>{},true],["👨‍🏫","Professeur — disponible après présentation",()=>{},true],["👁","Aperçu",onPreview,false],["✓","Présenter la solution",onSubmit,false]] as const;return <nav className="tool-rail" aria-label="Outils du tableau">{items.map(([icon,label,action,disabled])=><button key={label} type="button" title={label} aria-label={label} disabled={disabled} onClick={action}>{icon}</button>)}</nav>}
