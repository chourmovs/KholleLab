import katex from "katex";
export function MathContent({content}:{content:string}){const tokens=content.split(/(\\\[[\s\S]*?\\\]|\\\([\s\S]*?\\\))/g);return <div className="math-content">{tokens.filter(Boolean).map((token,index)=>{const display=token.startsWith("\\[");const inline=token.startsWith("\\(");if(display||inline){const expression=token.slice(2,-2);return <span key={index} className={display?"math-display":"math-inline"} dangerouslySetInnerHTML={{__html:katex.renderToString(expression,{displayMode:display,throwOnError:false,strict:"warn"})}}/>}return token.split(/\n\n+/).map((paragraph,i)=><p key={`${index}-${i}`}>{paragraph}</p>)})}</div>}
